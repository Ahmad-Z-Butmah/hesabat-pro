services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hesabat_db
    ports:
      - "5433:5432"
    volumes:
      - hesabat_pgdata:/var/lib/postgresql/data

volumes:
  hesabat_pgdata:
